# Indexes increase the performance of fetch queries, such as find() methods
# However dont over use them becouse they penalize insert and update operations

# Expires sessions only works with data fields